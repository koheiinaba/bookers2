<thead>
  <tr>
    <th></th>
    <th>name</th>
    <th></th>
    <th>comment</th>
  </tr>
</thead>
<tbody>
  <tr>
  <% books.book_comments.each do |book_comment| %>
    <td></td>
    <td><%= book_comment.user.name %></td>
    <td></td>
    <td><%= book_comment.comment %></td>
    <% if book_comment.user_id == current_user.id %>
    <td><%= link_to "Destroy", book_book_comments_path(book_comment.id), method: :delete, remote: true %>
    <% end %>
    </td>
  </tr>
  <% end %>
</tbody>